const prefix = 'shape'

export const SHAPE = {
  PARAM_INPUT: `${prefix}-param-input`,
  PARAM_PLUS: `${prefix}-param-plus`,
  PARAM_PANEL: `${prefix}-param-panel`
}

/**
 * @type {ReadonlyArray<string>}
 */
export const COLLECTION_SHAPE = [SHAPE.PARAM_PANEL]
/**
 * @type {ReadonlyArray<string>}
 */
export const VARIABLE_SHAPE = [SHAPE.PARAM_INPUT]
